import { DashboardPage } from './features/dashboard';

function App() {
  return (
    <div className="app-container">
      {/* For now, we just show the dashboard directly */}
      <DashboardPage />
    </div>
  );
}

export default App;